// TODO: remove
export type StudentOfGroup = {
  id: string
  groupId: string
  studentId: string
  // Time in milliseconds (UTC)
  startDate: number
  // Time in milliseconds (UTC)
  endDate: number | null
}
